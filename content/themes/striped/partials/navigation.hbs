<!-- Logo -->
<h1 id="logo">
    <a href="{{@blog.url}}">
        {{ @blog.title }}

        <!--{{#if @blog.logo}}-->
            <!--<img class="image fit featured" src="{{@blog.logo}}" alt="{{@blog.title}}" />-->
        <!--{{/if}}-->
    </a>
</h1>

<!-- Nav -->
<nav id="nav">
    <ul>
        {{#foreach navigation}}
            <li
                class="{{#if current}} current{{/if}}">
                <a href="{{url absolute="true"}}">
                    {{label}}
                </a>
            </li>
        {{/foreach}}
    </ul>
</nav>

sss
{{tags}}
